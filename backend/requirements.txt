# Note: nfl_data_py==0.3.3 is intentionally NOT listed here. Its own
# metadata pins numpy<2 and pandas<2, but pandas 1.5.x has no Python 3.12
# wheel and fails to build from source. It runs fine against the modern
# pandas/numpy pinned below in practice. Install it separately with
# `pip install nfl_data_py==0.3.3 --no-deps` AFTER this file (see
# backend/Dockerfile and README) so pip doesn't try to downgrade pandas/numpy.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.6.0
xgboost==2.1.3
joblib==1.4.2
appdirs==1.4.4
fastparquet==2024.11.0
httpx==0.28.1
pytest==8.3.4
pytest-cov==6.0.0
